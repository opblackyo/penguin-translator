# Contributing

## Workflow

1. 從 `main` 建立 `chore/`、`feat/`、`fix/`、`test/`、`docs/` 或 `refactor/` 分支。
2. 每個 Commit 保持單一目的，使用 Conventional Commits。
3. 不提交 secret、模型權重、cache、私人漫畫或來源不明的圖片。
4. 送出變更前執行 README 中的格式、lint、型別、測試與建置命令。

Pull Request 應說明目的、範圍、非範圍、測試結果、安全與隱私影響、人工驗證步驟與回滾方式。
