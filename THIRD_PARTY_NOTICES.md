# Third-Party Notices

本專案的 JavaScript 與 Python 套件來源及精確版本記錄於 `pnpm-lock.yaml` 和 `services/api/uv.lock`。各套件仍適用其自身授權條款。

直接執行時相依包含 Preact、Vite、FastAPI、Pydantic、Uvicorn、HTTPX、Pillow 與 Google
Gen AI SDK。M1 的明確 opt-in OCR 相依包含 PaddleOCR、PaddlePaddle 與 PaddleX；模型權重不
屬於 Repository。開發工具包含 pnpm、Biome、TypeScript、Vitest、Playwright、uv、pytest、
Ruff 與 Pyright。在首個 Release 前必須以 lockfile 再產生並審查完整清單。
