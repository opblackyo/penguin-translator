# Third-Party Notices

本專案的 JavaScript 與 Python 套件來源及精確版本記錄於 `pnpm-lock.yaml` 和 `services/api/uv.lock`。各套件仍適用其自身授權條款。

M0 直接執行時相依包含 Preact、Vite、FastAPI、Pydantic 與 Uvicorn；開發工具包含 pnpm、Biome、TypeScript、Vitest、Playwright、uv、pytest、Ruff 與 Pyright。在首個 Release 前必須以 lockfile 再產生並審查完整清單。
